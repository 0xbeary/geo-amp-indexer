# Geo Protocol Testnet EVM RPC Provider
#
# This provider connects to the Geo Protocol testnet chain
# Chain ID: 19411

kind = "evm-rpc"
network = "geo_testnet"

# RPC endpoint URL
url = "https://rpc-geo-test-zc16z3tcvf.t.conduit.xyz"

# Connection settings
concurrent_requests = 50
batch_size = 100

# Rate limiting (requests per second) - increased for faster sync
rate_limit = 50
